// import React from 'react';
// import { shallow } from 'enzyme';

// import LoadingStatusContainer from '../index';

describe('<LoadingStatusContainer />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
